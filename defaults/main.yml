---

es_cluster_name: elasticsearch
es_node_name:    "{{inventory_hostname}}"
es_node_master:  true
es_node_data:    true

es_index_num_shards:   5
es_index_num_replicas: 1

es_path_logs: /var/log/elasticsearch
es_path_data: 
  - /var/lib/elasticsearch

es_host: "{{ansible_default_ipv4.address}}"

