---
- hosts: all
  sudo: yes

  vars:
    - es_version: 2.1.1
    - es_plugins:
        - { name: cloud-aws, src: cloud-aws }
        - { name: head,      src: mobz/elasticsearch-head }

  roles:
    - ansible-role-elasticsearch
