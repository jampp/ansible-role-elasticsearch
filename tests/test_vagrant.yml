---
- hosts: all
  sudo: yes

  vars:
    - es_version: 2.1.1
    - es_upgrade: true
    - es_plugins:
        - { name: head, src: mobz/elasticsearch-head }

  roles:
    - ansible-role-elasticsearch
